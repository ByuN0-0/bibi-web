interface:
  display_name: "비비 LoL 내전 기록 저장"
  short_description: "점수판 스크린샷을 즉시 비비웹 검토 기록으로 저장"
  brand_color: "#C89B3C"
  default_prompt: "Use $bibi-ingest-lol-match to mechanically ingest this LoL scoreboard immediately and return only the admin review link."
policy:
  allow_implicit_invocation: true
